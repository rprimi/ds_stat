---
title: "Exercicio3"
format: html
editor: source
---

## Importa base
bla bla bal   


```{r}
library(readr)

getwd()
enade <- read_csv("enem.csv")
View(enade)

```

```{r}

enade <- readRDS("~/Dropbox (Personal)/Ciencia_de_dados_stat/ds_stat/data_exerc/enem.RDS")

fit <- t.test(nu_idade ~ tp_sexo, data =  enade )

round(fit$p.value, 70)

enade$tp_semestre

names(enade)
enade[, 1 ]
colunas <- c(2, 9, 10)
enade[ ,  colunas]

base::

library(dplyr)

select(enade, co_grupo, nu_idade, tp_sexo)

resumo <- enade %>%
 select(co_grupo, nu_idade, tp_sexo) %>%
 skimr::skim()

table(enade$tp_sexo)
mean(enade$nu_idade)
names(enade)
names(enem)

save.image("exercicio3.RData")
names(enade)
str(enade)

str(enade)
```
